#!/usr/bin/env bash

set -euo pipefail

which rg > /dev/null

if rg --glob '!bin/forbid' --ignore-case 'dbg!|fixme|todo|xxx'; then
  echo ripgrep found things
  exit 1
else
  echo ripgrep didnt find things
  exit 0
fi
