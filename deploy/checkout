#!/usr/bin/env bash

set -euxo pipefail

if [[ ! -d ord ]]; then
  git clone https://github.com/casey/ord.git
fi

cd ord

git fetch origin
git checkout -B master
git reset --hard origin/master
./deploy/setup
