#!/usr/bin/env bash

set -euxo pipefail

BRANCH=$1
DOMAIN=$2

if [[ ! -d ord ]]; then
  git clone https://github.com/casey/ord.git
fi

cd ord

git fetch origin
git checkout -B $BRANCH
git reset --hard origin/$BRANCH
./deploy/setup $DOMAIN
