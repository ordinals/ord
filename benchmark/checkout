#!/usr/bin/env bash

set -euxo pipefail

REV=$1

if [[ ! -d ord ]]; then
  git clone https://github.com/casey/ord.git
fi

cd ord

git fetch --all --prune
git branch | grep -v master | xargs git branch -D
git checkout $REV
./benchmark/run
